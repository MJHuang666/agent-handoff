# Participant Profiles

Create one Profile per immutable `(participant_id, tool, role)` tuple from `_templates/participant.md`. If one tool has multiple identities, the session must ask which participant ID it represents.
