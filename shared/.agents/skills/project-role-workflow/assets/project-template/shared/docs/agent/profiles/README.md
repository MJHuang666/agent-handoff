# Participant Profiles

Profile 保存参与者身份和项目差异，通用职责从 `../roles/` 读取。文件路径为 `<tool>/<participant_id>.md`。

注册新身份时复制 `_templates/participant.md`，填写真实字段并在 `../../role-bindings.md` 登记。participant_id、tool、role 创建后不可改变；停用身份保留文件和历史引用。
